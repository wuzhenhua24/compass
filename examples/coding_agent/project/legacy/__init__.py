"""Superseded modules, kept for reference during the reporting migration.

Nothing in the live code path imports from here. The package is still in the
tree because the finance exports have not been re-run against the new
reporting code yet, and until they have, the old implementations are the only
record of how last year's numbers were produced.
"""
